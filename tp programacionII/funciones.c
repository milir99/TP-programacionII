#include <stdio.h>
#include <stdlib.h>
#define "funciones.h"
nodoIngresos * alta_de_ingreso(nodoIngresos ingresos)
{

    int existencia=existePaciente(ingresos);

    if(existencia==0)
    {
        printf("el Paciente no existe.");

    }
    else
    {
        nodoIngresos*nuevoNodo=crearNodoIngreso(ingreso)
    }
}
